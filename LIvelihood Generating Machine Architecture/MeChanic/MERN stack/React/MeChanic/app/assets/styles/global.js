import { StyleSheet } from "react-native";

export const globalStyles = StyleSheet.create({
    container: {
        flex: 1,
        padding: 7,
    },
});

export const images = {
    cars: {
        '2021CadillacCT4': require('../cars/2021CadillacCT4.png')
    }
}